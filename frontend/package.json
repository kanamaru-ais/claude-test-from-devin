{
  "name": "task-manager-frontend",
  "version": "1.0.0",
  "description": "タスク管理アプリ フロントエンド",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "jsdom",
    "testMatch": ["**/tests/**/*.test.ts"],
    "globals": {
      "ts-jest": {
        "tsconfig": {
          "module": "commonjs"
        }
      }
    }
  }
}
